#include <bits/stdc++.h>

using namespace std;

int main()
{
    string linha;
    cin >> linha;
    for(int i = 0; i < linha.size(); i++)
    {
        char letra = linha[i];
        if(letra[i])
    }
}
