#include <iostream>
#include <cmath>

using namespace std;
int main()
{
    double r1, b, c, k, s;
    cin >> b >> c >> k >> s;
    r1 = (b * c) + ((2 / k) / (2 * s));
    cout << r1 << endl;
    return 0;
}
