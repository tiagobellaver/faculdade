#include <iostream>
#include <cmath>

using namespace std;

int main()
{
    double tt, a, b, c;
    cin >> a >> b >> c;
    tt = (4 / 2 * a) * (sqrt(2 * pow(c,2))) / (b);
    cout << tt << endl;
    return 0;

}
